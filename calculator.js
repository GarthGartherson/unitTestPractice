const calculator = {
  add(numX, numY) {
    return numX + numY;
  },
  subtract(numX, numY) {
    return numX - numY;
  },
  multiply(numX, numY) {
    return numX * numY;
  },
  divide(numX, numY) {
    return numX / numY;
  },
};

module.exports = calculator;
